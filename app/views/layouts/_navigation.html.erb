<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to (image_tag("logo.svg")), root_path, id: "logo", class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <% unless current_page?(root_path) || current_page?(posts_path)%>
        <%= render "layouts/search" %>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Share an Idea", new_post_path, class: "btn navbar-btn button", type: "button" %></li>
        <% if user_signed_in? %>
          <li class="dropdown">
            <a class="dropdown-toggle btn navbar-btn button" data-toggle="dropdown" href="#">
              <%= current_user.name %> <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Account", edit_user_registration_path%></li>
              <li><%= link_to('Logout', destroy_user_session_path, :method => :delete) %></li>
            </ul>
          </li>
        <% else %>
          <%= link_to "Sign in", new_user_session_path, class: "btn button navbar-btn", type: "button" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "btn button navbar-btn", type: "button" %>
        <% end %>
        
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>